- name: Copy elastic search repo file
  ansible.builtin.template:
    src: elastic.repo
    dest: /etc/yum.repos.d/elastic.repo

- name: restart ELK
  ansible.builtin.yum:
    name: filebeat
    state: installed

  